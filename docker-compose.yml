# Run the dashboard locally with: docker compose up --build
# Then open http://localhost:8501
services:
  dashboard:
    build: .
    image: late-delivery-risk-prediction:latest
    container_name: late-delivery-risk-dashboard
    ports:
      - "8501:8501"
    restart: unless-stopped
